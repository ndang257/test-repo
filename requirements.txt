# Required Software
mysql == 8.1.0
Python == 3.14.0

# Required Python Libraries
sqlalchemy == 2.0.44
pandas == 2.3.3 # data manipulation and analysis library.
numpy == 2.3.5 # fundamental package for scientific computing with Python.
scipy == 1.17.0 # mathematical calculations and functions built on NumPy. The stats subpackage enables a variety of statistical calculations.
typing == # defines a standard notation for Python function and variable type annotations
pymysql == 1.4.6 # pure-Python MySQL client library.
matplotlib == 3.10.7 # plotting library for creating static, animated, and interactive visualizations in Python.
seaborn == 0.13.2 # statistical data visualization library based on Matplotlib.
random ==  # implements pseudo-random number generators for various distributions.
datetime == 6.0 # provides classes for manipulating dates and times.
os == # provides a way to interact with the operating system
subprocess == # provides a way to spawn new processes, connect to their input/output/error pipes, and obtain their return codes.
collections == # provides specialized container datatypes. Used here to count hashable objects.
itertools == # provides a collection of tools for working with iterators. Used here to construct combinations of products.

